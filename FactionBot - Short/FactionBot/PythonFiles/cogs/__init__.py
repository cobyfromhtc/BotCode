# -*- coding: utf-8 -*-
"""FactionBot cogs package.

Loaded from Bot.py setup_hook via `await bot.load_extension("cogs.<name>")`.
Each module defines `async def setup(bot)` per the discord.py extension
contract. The shared helpers live in `cogs.botkit`.
"""
